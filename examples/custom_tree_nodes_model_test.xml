<root>
  <TreeNodesModel>
    <Condition ID="IsDoorClosed">
      <input_port name="door_id"/>
    </Condition>

    <Condition ID="IsDockAligned">
      <input_port name="docking_station"/>
    </Condition>

    <Action ID="OpenDoor">
      <input_port name="door_id"/>
      <input_port name="timeout"/>
    </Action>

    <Action ID="CloseDoor">
      <input_port name="door_id"/>
    </Action>

    <Action ID="NavigateThroughDoor">
      <input_port name="door_id"/>
      <input_port name="goal"/>
    </Action>

    <Action ID="AlignToDock">
      <input_port name="docking_station"/>
    </Action>

    <Action ID="DockRobot">
      <input_port name="docking_station"/>
    </Action>

    <Decorator ID="RetryDoorOpeningDecorator">
      <input_port name="num_attempts"/>
    </Decorator>

    <Control ID="DoorMissionSequence"/>
  </TreeNodesModel>
</root>